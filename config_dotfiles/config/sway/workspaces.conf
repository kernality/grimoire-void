# Portable starting point: applications open on the focused workspace.
# Add personal names/assignments only after checking IDs with swaymsg -t get_tree.
workspace_layout default
include ~/.config/sway/workspaces.local.conf
